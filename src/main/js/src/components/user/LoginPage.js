import React, { PropTypes } from 'react';
import { observer } from 'mobx-react';

const LoginPage = () => {
  return (
    <div>LoginPage</div>
  );
};

LoginPage.propTypes = {};

export default observer(LoginPage);
